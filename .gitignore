# =============================================================================
# CareTip — React + Vite + Node.js + PostgreSQL (Prisma) + Supabase
# =============================================================================

# --- Dependencies ---
node_modules/

# --- Build output ---
dist/
dev-dist/
backend/dist/
build/
out/
*.tsbuildinfo

# --- Vite / frontend caches ---
.vite/
.eslintcache
.cache/
.parcel-cache/

# --- Environment (commit *.example only) ---
.env
.env.*
!.env.example
!**/.env.example

# --- Logs & temp ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tmp
*.temp
tmp/
temp/

# --- OS & editor junk ---
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*.swo
*~

# --- IDE (team-shared .vscode may stay tracked) ---
.idea/
.cursor/
.qodo/
*.suo
*.ntvs*
*.njsproj
*.sln

# --- Markdown: local audits only; allowlisted docs stay tracked ---
*.md
!/README.md
!/github-cleanup-audit.md
!/disabled-workflows-report.md
!/dashboard-ux-audit.md
!/mobile-dashboard-audit-v2.md
!/dashboard-mobile-responsiveness-audit.md
!/navigation-flash-redirect-audit.md
!/content-reduction-report.md
!/auth-root-cause-analysis.md
!/auth-navigation-audit-v2.md
!/auth-mobile-consistency-audit.md
!/auth-performance-report-v2.md

# --- Local restore / accidental paste dumps ---
.restore_*.txt
*.restore_*_report.txt
Untitled

# --- Secrets & cloud credentials (never commit) ---
FIREBASE_SERVICE_ACCOUNT_JSON_COPY.md
caretip-service-account.txt
caretip-firebase-sa.txt
*service-account*.txt
*service-account*.json
*firebase*sa*.txt
firebase-service-account.json
**/firebase-service-account.json
*.pem
*.p12
*.key
!**/*.key.example

# --- Firebase local emulator / debug ---
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# --- Supabase CLI (local linked project state) ---
.supabase/
supabase/.branches/
supabase/.temp/

# --- PostgreSQL / database dumps (local backups) ---
*.sql.gz
*.dump
*.backup
*.db
*.sqlite
*.sqlite3
pgdata/

# --- Docker local overrides ---
docker-compose.override.yml
docker-compose.override.yaml

# --- User uploads & generated media (use Supabase Storage in prod) ---
backend/uploads/
**/uploads/
!**/uploads/.gitkeep

# --- Test, coverage & E2E (Playwright) ---
coverage/
.nyc_output/
/test-results/
/playwright-report/
/blob-report/
playwright/.cache/
playwright/.auth/

# --- Postman / Newman (commit collection + example env only) ---
caretip-postman-environment.json
postman-environment.local.json
newman-results.json
postman-cookies.json
verification-results.json

# --- GDPR / compliance exports ---
GDPR*
*GDPR*

# --- Documentation exports & design audit screenshots ---
docs/**/*-audit/
docs/**/hero-layout-audit/
caretip_adjustment.pdf/
caretip-translations.pdf/

# --- Large duplicate raster assets (enable after WebP migration) ---
# After `git rm --cached images/*.png` for pairs that have .webp siblings:
# images/**/*.png
# !images/bizzy001.png

# --- Deploy platform local config ---
.vercel/
.netlify/
.render/
