/**
 * shadcn-style Card primitives (same as `card.tsx`).
 * Kept as `hero-195-1` for compatibility with block copy-paste naming.
 */
export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardDescription,
  CardContent,
} from "./card";
