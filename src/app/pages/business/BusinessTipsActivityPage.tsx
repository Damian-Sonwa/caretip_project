import { TipsActivityPage } from "../shared/TipsActivityPage";

/** Business dashboard route — tips & activity ledger (lazy-safe, no route props). */
export function BusinessTipsActivityPage() {
  return <TipsActivityPage variant="default" />;
}
