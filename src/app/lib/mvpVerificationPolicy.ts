/**
 * MVP launch policy: platform admin approval gates go-live; KYC document upload UI is optional/future.
 * APIs and schema remain — flip to `true` when document upload becomes a product requirement.
 */
export const MVP_KYC_DOCUMENT_UPLOAD_ENABLED = false;
